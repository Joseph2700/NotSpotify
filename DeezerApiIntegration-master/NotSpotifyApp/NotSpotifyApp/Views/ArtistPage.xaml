<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             NavigationPage.HasNavigationBar="False"
             x:Class="NotSpotifyApp.Views.ArtistPage">
    
    <ContentPage.Content>
        <ScrollView>


            <StackLayout Margin="30">

                <Label Text="Insert the artist ID"
                       HorizontalOptions="Center"
                       FontAttributes="Bold"
                       FontSize="40"
                       TextColor="Black"/>

                <Entry     Keyboard="Numeric"
                           Placeholder="Enter the artist's Deezer ID here" 
                           Text="{Binding Id}" 
                           FontSize="15" 
                           VerticalOptions="Center">
                </Entry>

                <Button  
                        Text="Show artist info" 
                        BorderColor="AliceBlue" 
                        BackgroundColor="CadetBlue" 
                        FontAttributes="Bold" 
                        CornerRadius="50" 
                        Command="{Binding GetArtistInfoCommand}" 
                        HorizontalOptions="CenterAndExpand">
                </Button>

                <Image Source="{Binding ArtistInfo.PictureXL}" HorizontalOptions="FillAndExpand"/>
                <Label Text="{Binding ArtistInfo.Name}" FontAttributes="Bold" FontSize="30" TextColor="BurlyWood" HorizontalOptions="StartAndExpand"/>

                <Label Text="Number of fans: " FontSize="17"/>
                <Label Text="{Binding ArtistInfo.NumberOfFans}" FontAttributes="Bold" FontSize="18"/>

                <Label Text="Deezer link: " FontSize="17"/>
                <Label Text="{Binding ArtistInfo.Link}" FontAttributes="Bold" FontSize="18" />

                <Label Text="Sharing link: " FontSize="17"/>
                <Label Text="{Binding ArtistInfo.Share}" FontAttributes="Bold" FontSize="18"  />

                <Label Text="Number of albums: " FontSize="17"/>
                <Label Text="{Binding ArtistInfo.NumberOfAlbums}" FontAttributes="Bold" FontSize="18"/>

                <Label Text="Artist radio: " FontSize="17"/>
                <Label Text="{Binding ArtistInfo.Radio}" FontAttributes="Bold" FontSize="18" />

                <Label Text="Sharing link: " FontSize="17"/>
                <Label Text="{Binding ArtistInfo.Tracklist}" FontAttributes="Bold" FontSize="18"/>
            </StackLayout>
        </ScrollView>

    </ContentPage.Content>
</ContentPage> 