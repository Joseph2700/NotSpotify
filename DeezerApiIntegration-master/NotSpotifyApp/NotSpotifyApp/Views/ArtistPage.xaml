<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             NavigationPage.HasNavigationBar="False"
             x:Class="NotSpotifyApp.Views.ArtistPage">
    
    <ContentPage.Content>
        <ScrollView>


            <StackLayout Margin="30">

                <Label Text="Insert the artist ID"
                       HorizontalOptions="Center"
                       Style="{StaticResource Title}"/>

                <Entry     Keyboard="Numeric"
                           Placeholder="Enter the artist's Deezer ID here" 
                           Text="{Binding Id}" 
                           FontSize="15" 
                           VerticalOptions="Center">
                </Entry>

                <Button  
                        Text="Show artist info" 
                        BorderColor="AliceBlue" 
                        BackgroundColor="CadetBlue" 
                        FontAttributes="Bold" 
                        CornerRadius="50" 
                        Command="{Binding GetArtistInfoCommand}" 
                        HorizontalOptions="CenterAndExpand">
                </Button>

                <Image Source="{Binding ArtistInfo.PictureXL}" 
                       HorizontalOptions="FillAndExpand">
                </Image>
                
                    <Label Text="{Binding ArtistInfo.Name}" FontAttributes="Bold" FontSize="30" TextColor="DarkGoldenrod" HorizontalOptions="CenterAndExpand"/>

                <Label Text="Number of fans: " Style="{StaticResource InfoLabel}"/>
                <Label Text="{Binding ArtistInfo.NumberOfFans}" Style="{StaticResource Information}"/>

                <Label Text="Number of albums: " Style="{StaticResource InfoLabel}"/>
                <Label Text="{Binding ArtistInfo.NumberOfAlbums}" Style="{StaticResource Information}"/>

                <Label Text="Deezer link: " Style="{StaticResource InfoLabel}"/>
                <Label Text="{Binding ArtistInfo.Link}" Style="{StaticResource Information}"/>

                <Label Text="Sharing link: " Style="{StaticResource InfoLabel}"/>
                <Label Text="{Binding ArtistInfo.Share}" Style="{StaticResource Information}"/>

                <Label Text="Sharing link: " Style="{StaticResource InfoLabel}"/>
                <Label Text="{Binding ArtistInfo.Tracklist}" Style="{StaticResource Information}"/>
            </StackLayout>
        </ScrollView>

    </ContentPage.Content>
</ContentPage> 